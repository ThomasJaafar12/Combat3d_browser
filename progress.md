Original prompt: Build a 3D in-browser RPG combat prototype with a leader, three-slot spell loadout, autonomous companions, group orders, shared authoritative combat systems, reward flow, required docs, and disciplined commit slices.

## Progress log
- Audited the repo: this branch contains curated assets only, not the referenced starter runtime.
- Cloned the referenced starter into `starter_reference/` for architecture reference only; it is not part of the target deliverable.
- Confirmed local Node is available, but Rust and SpacetimeDB are not installed in this environment.
- Decided on a V0 path: React/R3F client plus a clean authoritative simulation boundary that can later be swapped to the intended server layer.
- Commit 1 target: scaffold `client/`, add feature flags, initialize docs, and record the asset map before combat implementation begins.
- Verified Commit 1 with `npm.cmd run build`.
- Started the Vite dev server and ran the required Playwright verification loop against `http://127.0.0.1:5173`.
- Added `window.render_game_to_text` and `window.advanceTime` hooks so later combat work stays testable.
- Fixed an initial camera/framing issue after the first screenshot came back visually blank; the latest screenshot now shows the scaffold geometry and label correctly.
- Added shared schema definitions for factions, units, weapons, spells, statuses, orders, rewards, loadouts, and behavior profiles.
- Added centralized curated asset URL bindings so the upcoming runtime can consume existing assets without duplicating them.
- Added the first V0 content catalog: 7 units, 7 weapons, 9 spells, 7 statuses, 6 orders, 6 rewards, and 3 behavior profiles.
- Updated `render_game_to_text` to expose the shared catalog summary for browser verification.
- Added runtime math helpers, arena/map data, and the first authoritative combat engine class.
- The app shell now subscribes to live authority snapshots instead of rendering only static scaffold text.
- Engine foundation covers encounter reset, recruit/start-battle flows, HP/resource/cooldown ticking, shield/status upkeep, downed/death transitions, XP gain, and reward choice gating.
- Replaced the static canvas with a live combat scene that renders units, obstacles, zones, projectiles, floating text, and a third-person follow camera.
- Added leader input plumbing: WASD/arrow movement, right-drag camera, click-to-select/basic attack, spell arming on `1-3`, and revive on `R`.
- Added scoped companion orders (`all`, archetype groups, custom groups) and a functional behavior editor for stance/profile, custom group, and heal/retreat thresholds.
- Reward choices now apply persistent run bonuses and return the player to loadout for replay instead of acting as dead-end UI.
- Added `client/src/game/assets/loader.ts` to cache FBX/GLTF presentation assets, normalize them to runtime scale, and clone scene instances without touching simulation logic.
- Moved arena data into `client/src/game/environment/arena.ts`, added a real ground asset, and swapped the scene from primitive capsules/boxes to curated character/environment models with lightweight animation selection.
- Added `client/src/game/input/orderTargeting.ts` and wired `Q` / `H` / `T` plus setup buttons to click-to-place `defend_area`, `hold_position`, and `retreat` anchors.
- Reworked the viewport UI into `client/src/components/HUD/CombatHUD.tsx`, adding a leader panel, companion panel, target panel, reward panel, and a clearer action bar while leaving authority state ownership unchanged.
- Fixed one curated prop asset bug by removing broken texture references from `assets/game/environment/arena_outdoor/props/env_prop_fence_metal_01.gltf`.
- Added a battlefield-aware camera pass that biases framing toward the engagement point, widens default zoom/pitch, and applies lightweight obstacle avoidance instead of only looking a few meters in front of the leader.
- Verified the new slice with repeated `npm.cmd run build` runs and Playwright screenshot/state captures against `http://127.0.0.1:4173`.
- Current validation status: curated models, HUD, and the improved camera render in-browser, but the automated mobile-sized capture still favors the center lane and one GLTF texture warning may still be cached by the running dev server.
- Locked the app shell to the viewport height so the main game window no longer forces page scrolling; only the setup/debug column scrolls when it overflows.
- Disabled wheel zoom for now and kept the camera at a fixed third-person distance.
- Hardened leader movement input by normalizing both physical key codes and typed letters (`WASD`, arrows, and `ZQSD`), auto-focusing the viewport, and listening in the keyboard capture phase so UI focus is less likely to swallow movement keys.
- Fixed the authority update loop so leader movement advances during both `loadout` and `battle`; Playwright validation now shows the leader position changing in `loadout` before battle start.
- Investigated a perceived third-person inversion bug and confirmed the camera rig was not the primary fault. The actual issues were an inverted local-to-world movement basis plus an unnecessary 180-degree presentation yaw offset on the character models.
- Corrected movement so forward input maps to camera-forward on the ground plane, fixed the yaw rotation matrix in authority movement, and removed the model-side 180-degree rotation offset so units face the direction of travel/action.
- Separated unit overlay labels/bars/selection markers from the rotating model transform so they stay readable while the character turns.
- Fixed the remaining strafe inversion by flipping the local horizontal input sign so `A` / left now maps to camera-left and `D` / right maps to camera-right.
- Identified the main CPU bottleneck as snapshot churn: the authority was cloning and publishing a full immutable snapshot every simulation step, and the UI consumed those updates directly. Throttled `advanceTime` snapshot emission to 30 Hz while preserving immediate forced emits for commands and phase changes.
- Identified the main GPU bottleneck as overkill render settings for the current scene: dynamic shadow mapping plus a higher-resolution antialiased canvas. Switched the canvas to a capped DPR, disabled antialiasing, and removed the real-time shadow light path.
- Validation after the liveops pass: `npm.cmd run build` passes, the left-strafe Playwright capture now ends with the leader at negative X (`x: -4.7`), and the scene remains visually synchronized after removing the extra App-side snapshot queue.
- Added an in-viewport live-ops panel with FPS, frame time, simulation snapshot rate, draw calls, triangle count, unit count, and FX count so runtime health is visible without opening devtools.
- Optimized the hot render path further by memoizing stable scene pieces (`ArenaGround`, `ArenaObstacles`, unit labels/health bars, and unit models), deferring the large debug shell with `useDeferredValue`, and removing avoidable per-frame camera collision allocations.
- The live Playwright capture now shows the overlay populated in-browser. Headless Chromium on SwiftShader still reports much lower FPS than a hardware-accelerated local browser, but draw calls are down to the teens and geometry cost remains low enough that remaining low numbers are dominated by the headless/software-render test environment rather than scene complexity.
- Investigated movement-specific local FPS drops. The strongest likely bottleneck was not scene geometry but browser compositing: large `backdrop-filter` UI layers were repainting over the moving 3D scene. Replaced those blurred translucent panels with opaque layered backgrounds and paint containment to reduce movement-time UI cost.
- Decoupled authority simulation from monitor refresh by switching the main loop to a fixed 60 Hz simulation step. This prevents 144 Hz / 240 Hz displays from multiplying CPU cost just because `requestAnimationFrame` fires more often.
- Raised snapshot publication from 30 Hz to 60 Hz after the earlier render/UI reductions so motion updates stay more responsive without returning to the previous full-churn behavior.
- Added a presentation-event seam to the authority snapshot so audio and lightweight visual polish can react to combat outcomes without owning gameplay state.
- Added `client/src/game/audio/audioSystem.ts` as a fail-soft browser audio bridge and wired attack/cast/impact/downed/revive/level-up/UI confirmation cues from the app shell.
- Expanded the smoke coverage into `tests/combat.smoke.ts` / `client/tests/combat.smoke.ts`, covering recruit -> battle start -> placed order -> ground spell -> revive -> wave clear -> reward selection. `npm.cmd run smoke -- http://127.0.0.1:5173` now passes.
- Added compact debug controls for god mode, cooldown bypass, AI labels, fast-forward, debug spawning, forced ally downing, and wave clearing, plus richer live readouts for AI state and cooldowns.
- Added lightweight combat-feedback polish: status icons over units, floating hit/reward sprites, and AI state labels behind the existing debug flag.
- Investigated the remaining arena GLTF texture warning, confirmed it only appeared on stale dev-server runs, then cleaned every arena-outdoor GLTF in-source to remove those broken material texture references entirely.
- Fresh validation on a clean `npm.cmd run dev -- --host 127.0.0.1 --port 5173` server: `npm.cmd run build` passes, `npm.cmd run smoke -- http://127.0.0.1:5173` passes, the Playwright screenshot/state capture updates cleanly, and a targeted Playwright console probe returns `[]` for console errors.
- Investigated a new left/right locomotion presentation bug where the visible rig drifted away from its label/selection marker during strafes. The cause was imported FBX root-motion tracks translating the hips inside the unit parent. Anchored horizontal root translation during clip normalization so presentation stays locked to the authoritative unit position while preserving vertical motion.
- Confirmed a second contributor to the same symptom: unit labels and health bars were not camera-facing, so strafing/orbiting could mirror them and make the overlay look detached from the character. Wrapped the overlay layer in a billboard so it stays visually attached and readable through left/right movement.
- Fixed the remaining locomotion mismatch in the authority/presentation seam: player movement was still forcing the leader to face travel direction, which made left/right input collapse into turn-and-run. The leader now keeps aim yaw while moving, and the presentation layer selects directional locomotion clips (`runLeft`, `runRight`, `runBack`) when the current rig exposes them.
- Investigated follow-up reports of the leader appearing under the floor and locomotion twitching. The combat coordinate system was still sane (`unit.position.y` remains on the ground plane via authority resolution); the real regression was presentation setup: directional locomotion clips were accidentally configured as one-shot actions, so they could clamp on a low end-pose and appear sunk/frozen. Marked all locomotion states as looping repeat animations and revalidated left-strafe captures.
- Confirmed the remaining sink/twitch only affected the extra side/backward clips imported from the separate paladin pack. Removed those directional clips from runtime selection and reverted non-forward movement to the known-good base `run` clip so left/right/backward movement stays stable on the current rig.
- Tuned the held-gear attachment transforms using the authored model axes: the captain sword/shield and ranger longbow now sit further off the rig with simpler rotations instead of the previous over-rotated poses that were collapsing them toward the body.
- Swapped the ranger visual to the string-equipped longbow variant and added a mild held-gear highlight so the attachment is readable in the battle/hold crop. The ranger is now the clearest validation target; the best evidence shot is `client/output/ranger-hold-brighter-crop.png`.
- Investigated the remaining “slightly under the map” look and found a separate geometry issue: the fitted ground slab was rendering above the simulation plane, so units at `y = 0` looked marginally buried. Also re-tested the paladin directional clips directly against the current leader body after the loop fix and confirmed they stay grounded with root X/Z anchored, so directional locomotion can be restored once the ground mesh is aligned to the actual play plane.
- Replaced the last fake flat-plane collision path with a dedicated environment collision helper. Authority movement now resolves through rotated obstacle bounds and a shared support-height layer instead of ad-hoc axis-aligned push-out, and units keep a small surface clearance above the floor to avoid visible foot clipping. Revalidated left movement plus a crate-approach scenario after wiring the resolver into `engine.ts`.
- Inspected the terrain system with uneven terrain in mind and removed the last hardcoded support-plane assumption. `ArenaDefinition` now carries authored `supportSurfaces`, and the collision sampler can resolve both flat regions and ramps with a step-height gate before applying obstacle collision. The current arena still uses a single flat support surface, but future raised pads/slopes can be added in data without changing the movement loop again.
- Reworked the camera rig into a true orbit around the leader pivot instead of the older forward-biased chase framing. The camera now rotates on a character-centered spherical offset and still keeps the obstacle avoidance pass, so right-drag orbit stays centered on the player rather than swinging around a point several meters ahead.
- Investigated the report that prior verification only captured post-input idle states. Added temporary presentation debug output to `render_game_to_text`, re-ran captures while movement keys were still held, and confirmed the animation selector was correct (`runLeft` / `runRight` / `runBack` were being chosen in motion).
- Traced the actual locomotion failure to asset-space mismatch, not selection logic: the side/backward clips come from the separate paladin rig, whose positional animation tracks are authored at a radically different scale than the leader rig. The leader forward run hips track animates around `y ~= 85-95`, while the paladin directional clips animate around `y ~= 0.9`, which was pulling the leader body down toward the floor when those clips were applied directly.
- Reworked `client/src/game/assets/loader.ts` so animation sources can declare a distinct source rig. Positional tracks are now normalized against the target model height before root X/Z anchoring, which makes mixed-source clips usable on the leader rig instead of assuming every FBX animation shares the same authored skeleton scale.
- Validation after the loader retarget fix: `npm.cmd run build` passes, the required `develop-web-game` Playwright client was rerun, and new in-motion captures in `client/output/live-anim-check/` show forward/left/right/backward while the keys are still held. The new state dumps confirm `presentationDebug.resolvedAnimation` is `run`, `runLeft`, `runRight`, and `runBack` respectively during motion.
- User clarified that the paladin pack should not have been replacing the leader at all. Cleaned that integration up by removing paladin clips from the leader and enemy-tank presentation configs, introducing a dedicated `companion_paladin` presentation for the Vanguard companion instead, and deleting the now-unneeded cross-rig animation retarget plumbing.
- Removed temporary `presentationDebug` wiring from `render_game_to_text` / `CombatScene`, and removed dead `UnitDefinition.modelUrl` metadata that had drifted out of sync and was not consumed anywhere in the runtime or scene.
- Updated asset audit character entries so the paladin pack is tracked as a companion asset instead of an implicit leader override. Validation after the cleanup: `npm.cmd run build` passes, the required `develop-web-game` Playwright client reruns cleanly with `[]` console errors, and a targeted recruit-Vanguard capture confirms the paladin asset now exists only through the companion path.
- Curated the served axeman animation set from `assetsv0/Pro Melee Axe Pack` into `assets/game/characters/enemies/` for the runtime states the current scene can actually play. Added directional locomotion (`runLeft`, `runRight`, `runBack`), a cast/ability stand-in (`battlecry`), and a dedicated block-react clip, while keeping the existing idle / run forward / horizontal attack / guard / gut hit clips as the primary base set.
- Wired the `enemy_melee` presentation to consume the fuller curated axe set in `assets.ts` / `loader.ts`, so the raider axeman now exposes `idle`, `run`, `runLeft`, `runRight`, `runBack`, `attack`, `cast`, `hit`, `block`, and `guard`. Removed a stale unused `axe.attackAlt` asset reference afterward.
- Validation for the axeman curation: `npm.cmd run build` passes, the required `develop-web-game` client reran cleanly, a runtime bundle inspection confirms `loadCharacterModel('enemy_melee')` now returns the full curated state set, and targeted battle captures load without console errors. The current camera framing still makes enemy pose review awkward in combat screenshots, but the asset bundle is live and complete for the present state machine.
- Corrected the archetype confusion on the leader: the player character is sword-and-shield, not axe. Copied the sword-and-shield pack into `assets/game/characters/leader/`, added the directional run clips plus the new walk clips for pure side/back input, and wired the leader presentation to the proper sword-and-shield asset set.
- Renamed the leader weapon entry from `captain_saber` to `captain_swordshield`, updated the HUD/content copy to match the sword-and-shield archetype, and swapped the `Honed Edges` reward icon to the sword icon instead of the axe icon.
- Validation for the sword-and-shield pass: `npm.cmd run build` passes in `client/`, the standard `develop-web-game` harness still runs cleanly with no console errors, and a custom held-key Playwright capture confirmed live forward/left/right/back movement while the key remained down. The forward frame was partially obscured by the archway in that capture, but the lateral/backward poses were clearly distinct and the leader position advanced in each case.
- Added the missing sword-and-shield walk set from the provided pack: `Standing Walk Left`, `Standing Walk Right`, and `Walking Backwards` now live under the leader presentation, and the movement selector uses them only for pure strafe/backpedal input.
- Added a leader-only `moveIntent` field to the runtime snapshot so the presentation layer can distinguish pure lateral input from diagonals. Pure `A`/`D` now uses the new walk clips, while `W+A`/`W+D` and other mixed inputs continue to use the run clips.
- Validation for the new walk split: `npm.cmd run build` passes in `client/`, the standard `develop-web-game` harness still exits with no console errors, and a held-key Playwright capture shows `moveIntent` resolving to pure side/back inputs for `A`/`D`/`S` while diagonal inputs keep both axes and do not trigger the walk clips.
- Added an `E` shortcut that toggles the leader's equipment slots at the authority boundary. The toggle clears `equipmentLoadout` to unequip sword/shield visuals and restores the curated default loadout on the next press.
- Updated the combat HUD copy so the control hint now advertises the new equipment toggle.
- Validation for the equipment toggle: `npm.cmd run build` passes, a fresh Playwright battle capture still loads cleanly, and a direct `KeyE` probe confirms the runtime state flips from `{"main_hand":"Captain Sword","off_hand":"Captain Shield"}` to `{}` and back again with no console errors.
- TODO: if the equipment system grows beyond the leader, replace the binary toggle with a real per-slot equipment picker/inventory flow.
- Collapsed the setup shell automatically on battle start and added an `Open setup` toggle so the full left-side debug/loadout panel no longer crowds the battle viewport.
- Shifted the left companions card and right live ops/debug panels lower so the battlefield view reads cleanly instead of stacking on top of the target/leader cards.
- Validation for the layout cleanup: `npm.cmd run build` passes, and a fresh Playwright battle capture now shows the setup shell hidden with the `Open setup` handle in battle state while the target/live-ops/debug cards no longer overlap the playfield.
- Investigated the missing off-hand shield in battle. The most likely cause was the shield anchoring to torso bones before the left arm, so it could end up hidden or off-camera. Reordered the off-hand attachment to prefer the left hand/forearm, switched the leader shield to the round shield asset for better silhouette readability, and disabled frustum culling on equipment meshes as a safety net before revalidating.
- Investigated the follow-up sword visibility complaint and confirmed the shared loader was still floor-centering every raw model. That normalization is appropriate for environment props but not for hand-held gear, so I split the clone path: characters and scenery still center on floor, while equipment now preserves its authored pivot before attachment.
- Implemented the ranger weapon path requested by the user: `ranger_longbow` now declares a visual attachment in content, the scene renders a weapon attachment only for weapons that opt in, and the bow asset is preloaded with the other held visuals. The validation crop now shows the Ranger carrying the bow-side geometry rather than trying to repurpose the leader’s suit mesh.

- Reworked the attachment pipeline foundation under `client/src/game/equipment/`: added explicit rig profiles, item definitions, stance profiles, attachment profiles, runtime rig adapters, a pure `EquipmentSystem`, and catalog validation instead of direct scene-level bone-parenting.
- Migrated runtime units from a raw visual `equipmentLoadout` blob to an explicit `equipmentState` with active slots, storage slots, and ready/stowed state. The leader equipment toggle now stows the sword/shield into storage representations and restores them from the default loadout instead of merely hiding the visuals.
- Rewired `CombatScene` to resolve socket nodes once per cloned character, bind item instances through the attachment registry, and expose dev attachment tooling: socket axes, marker axes, skeleton visualization, live localStorage-backed offset overrides, and attachment inspector reports in the debug overlay.
- Updated the HUD and `render_game_to_text` summary to report stance family plus active/stored equipment, and extended the smoke test to verify the new stow/restore behavior before battle start.
- Validation for the attachment rework: `npm.cmd run build` passes in `client/`, `npm.cmd run smoke -- http://127.0.0.1:5173` passes, a direct Playwright console probe on `http://127.0.0.1:5173` returns `[]`, and the required `develop-web-game` harness produced fresh captures in `client/output/attachment-rework-check-2/` with the leader sword/shield visible and no generated `errors-0.json`.
- Promoted the attachment tool from an inline debug panel into a dedicated editor mode overlay. The live debug card is now only a launcher and rig summary, while the actual authoring workspace takes over the viewport with its own unit/binding selection, preview stage, draft override state, explicit save/discard flow, and JSON export.
- The editor preview now updates attachment poses live without rebinding the model every time. `bindEquipmentModelToSocket` exposes both `applyPose` and `readPose`, the scene binding path uses incremental pose updates, and the dedicated editor writes to a draft override map until the user hits `Save and return`.
- Validation for the editor-mode pass: `npm.cmd run build` passes, `npm.cmd run smoke -- http://127.0.0.1:5173` passes after one retry of an existing flaky revive assertion, and a direct Playwright interaction confirmed `Open editor mode` opens the full overlay workspace and `Save and return` closes it back to the live view without console errors.
- Upgraded the attachment editor viewport for frame-accurate tuning: it now has play/pause, prev/next frame stepping, a sample-FPS selector, a scrub slider tied to clip time, and camera view presets (`iso/front/left/right/back`) on top of stronger orbit controls.
- Validation for the viewport pass: `npm.cmd run build` passes, `npm.cmd run smoke -- http://127.0.0.1:5173` passes, and Playwright confirmed the editor overlay exposes the new timeline controls, camera preset buttons, and frame stepping updates the displayed frame/time state.
- Performance findings for the editor mode pass: the current editor overlay mounts a second Three.js canvas instead of reusing or suspending the live battlefield canvas. Playwright confirms `document.querySelectorAll('canvas').length === 2` while the editor is open, and `render_game_to_text().timeMs` continues advancing in editor mode, so the authority tick and live battle render loop are still running underneath the editor.
- The built-in live ops readout currently reports only the battle renderer draw calls. In the observed loadout case that battle canvas sits around 27 draw calls, but the editor adds its own renderer plus helper-heavy preview content (`TransformControls`, `GridHelper`, socket axes, marker axes, optional `SkeletonHelper`, duplicated character + weapon scene), so the total GPU cost is materially higher than the panel suggests.
- Highest-priority optimization targets: suspend or unmount the live combat canvas/simulation while editor mode is active; stop feeding battle attachment-debug helpers under the editor; reduce editor helper defaults to selected socket/marker only; and move draft gizmo updates out of the root `App` state path so dragging does not cause whole-app rerenders.
- Implemented the first editor-mode performance pass. `App.tsx` now treats attachment editing as a true mode switch: while the editor is open, the live combat `Canvas`, HUD, live-ops card, and debug overlay are not rendered, and the authority tick loop short-circuits so battle time no longer advances underneath the editor.
- Hardened the interaction boundary so battlefield camera drag / wheel handlers do not run while the editor is active. This avoids bubbling editor interactions into the hidden live-view camera controller.
- Added editor-local render instrumentation and idle throttling in `AttachmentEditorViewport.tsx`. The preview now shows its own FPS / frame-ms / draw-call / triangle readout, and the editor `Canvas` switches to `frameloop=\"demand\"` whenever playback is paused and the gizmo is idle, so the preview stops burning frames when you are just inspecting a static pose.
- Validation for the optimization pass: `npm.cmd run build` passes in `client/`; `npm.cmd run smoke -- http://127.0.0.1:5174` passes after one rerun of the existing flaky revive assertion; the required `develop-web-game` harness ran against `http://127.0.0.1:5174`; Playwright confirms the editor now reports `canvasCount: 1` and `render_game_to_text().timeMs` remains frozen at `12850` across a 1s wait in editor mode; and the fresh capture in `client/output/attachment-editor-optimized.png` shows the standalone editor workspace with the new editor stats readout.
- Known residuals after the optimization pass: the editor still keeps the full root React tree mounted, draft pose updates still flow through `App` state during gizmo drags, and the console still shows the pre-existing `favicon.ico` 404 plus the FBX skin-weight warning. The next performance win should be throttling or localizing draft pose commits during drag and reducing helper draw calls further by defaulting to selected-socket/marker only.
- Added an editor-only orbit lock shortcut in `AttachmentEditorViewport.tsx`: pressing `Space` now toggles orbit controls on/off while the attachment editor is open, specifically to avoid conflicts between orbit rotation and transform-gizmo edits. The shortcut ignores focused form controls so typing in numeric fields or using buttons does not flip camera state accidentally.
- Validation for the orbit-lock QoL pass: `npm.cmd run build` passes in `client/`; the required `develop-web-game` harness reran against `http://127.0.0.1:5174`; and Playwright confirmed the editor hint changes from `Press Space to lock orbit` to `Press Space to resume orbit. Orbit locked.` on the first keypress, then back again on the second. No new console errors were introduced.
- Reworked the attachment editor to be archetype-driven instead of runtime-unit-driven. Once editor mode is open, the sidebar now lists every supported unit definition in the game catalog (leader, all companions, and all enemy archetypes), so tuning is no longer limited to whichever units happen to be alive in the current combat snapshot.
- Added the missing equipment item definitions and attachment profiles needed to make that broader picker useful: `vanguard_halberd_equip`, `mender_staff_equip`, and `hexcaster_staff_equip` now exist in the prototype catalog and equipment registry, and the corresponding unit archetypes now carry those items in their authored equipment loadouts.
- This keeps tuning asset-specific, not weapon-family-specific. Attachment overrides remain keyed by `EquipmentId`, so a future beginner sword and max-level sword can each have their own item entry and independent attachment profile even if both are classified as one-hand swords.
- Validation for the archetype/editor expansion: `npm.cmd run build` passes in `client/`; `npm.cmd run smoke -- http://127.0.0.1:5174` passes after one rerun of the existing flaky revive assertion; the required `develop-web-game` harness reran against `http://127.0.0.1:5174`; and Playwright confirmed the editor now lists `Iron Captain`, `Vanguard`, `Ranger`, `Mender`, `Raider Chaser`, `Hexcaster`, and `Bulwark Disruptor`, with `Mender` resolving to `mender_staff_equip` and `Hexcaster` resolving to `hexcaster_staff_equip` in the export payload.
- Applied a visual overhaul to editor mode using `mockup.html` as the design reference, without changing the feature set. The pass is mostly editor-scoped CSS: lavender workspace background, hard bordered white panels, mono labels, stronger segmented controls, denser/brutalist button styling, and a cleaner viewport stage/header treatment. The live game UI outside editor mode is intentionally untouched.
- Kept the pass performance-safe: no new runtime features were added, no extra preview canvases or helpers were introduced, and the previous editor-mode suspension / demand-frameloop behavior remains intact. The redesign specifically removed the old blur-heavy editor backdrop in favor of simpler flat gradients and hard shadows.
- Validation for the visual overhaul: `npm.cmd run build` passes in `client/`; `npm.cmd run smoke -- http://127.0.0.1:5174` passes; the required `develop-web-game` harness reran against `http://127.0.0.1:5174`; and a fresh Playwright capture at `client/output/attachment-editor-visual-overhaul.png` shows the new editor shell with the existing controls, viewport, and export panel preserved.
- Investigated the follow-up editor placement bug where Iron Captain sat partly below the floor while Ranger hovered above it. The root cause was the character clone normalization order in `client/src/game/assets/loader.ts`: clones were centered on the floor before height scaling, but the scale pass left final skinned bounds offset differently per rig. Re-centering character clones after the uniform-height scale brings all tested rigs back to `bounds.min.y ~= 0` in both combat and the attachment editor.

## TODO
- Improve camera readability further around large close-up props; the archway can still dominate the frame during some movements.
- The axe pack still does not include a dedicated death clip. The axeman presentation therefore has a complete movement / combat interaction set for the current runtime, but not a pack-native death state yet.
- Consider code-splitting the heaviest client chunk now that the runtime path is more stable and measurable.
- Expand smoke assertions further around companion autonomy and debug-toggle side effects if this prototype continues evolving.
- Add a dedicated single-asset preview scene / route for attachment authoring so profile editing does not have to share the live combat viewport.
- Promote the in-app attachment validator into a standalone CLI once the repo has a reliable Node path that can resolve the Vite alias graph without extra tooling.
